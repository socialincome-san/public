/**
 * Tailwind config stub. Theme is now defined in globals.css via @theme.
 * Kept for tooling compatibility (e.g. shadcn, IDE).
 */
import type { Config } from 'tailwindcss';

const config: Config = {
	content: ['./src/**/*.{ts,tsx}'],
};

export default config;
