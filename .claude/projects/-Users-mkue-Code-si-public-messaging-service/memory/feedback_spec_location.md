---
name: spec-location-preference
description: User prefers specs in ignored/ directory, not docs/superpowers/specs/
type: feedback
---

Save design specs to `ignored/specs/` directory, not `docs/superpowers/specs/`.

**Why:** User rejected creating docs/superpowers/specs/ directory. The `ignored/` directory is preferred for generated artifacts.

**How to apply:** When writing spec/design documents during brainstorming, use `ignored/specs/` as the target directory.
