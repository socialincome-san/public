[tools]
node = "24"
java = "23"

[tasks.install]
run = "npm ci"

[tasks.db]
run = "npm run db:up"

[tasks.mockserver]
run = "npm run mockserver:up"

[tasks.firebase]
run = "npm run firebase:serve"
depends = ["install"]

[tasks.build-paths]
run = "npm run website:build-paths"

[tasks.website]
run = "npm run website:serve"
depends = ["install", "db", "mockserver", "build-paths"]

[tasks.website-ssl]
run = "npm run website:serve:ssl"
depends = ["install", "db", "mockserver", "build-paths"]

[tasks.dev]
depends = ["firebase", "website"]

[tasks.dev-ssl]
depends = ["firebase", "website-ssl"]
