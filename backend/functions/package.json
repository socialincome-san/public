{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "yarn run build:watch | firebase emulators:start --project demo-social-income --config ../firebase.json --import ../../seed",
    "test": "jest",
    "emulators:test": "yarn run build && firebase emulators:exec --project demo-social-income --config ../firebase.json 'npm run test'",
    "format-code": "prettier --write ./"
  },
  "engines": {
    "node": "16"
  },
  "main": "lib/src/index.js",
  "dependencies": {
    "firebase-admin": "^10.0.2",
    "firebase-functions": "^3.21.0"
  },
  "devDependencies": {
    "@jest/globals": "^28.1.0",
    "@types/jest": "^27.5.0",
    "firebase-functions": "^3.21.0",
    "firebase-functions-test": "^2.0.1",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.1",
    "typescript": "^4.5.4"
  },
  "private": true
}
