import { DefaultPageProps } from '@/app/[lang]/[country]';
import { Typography } from '@socialincome/ui';

export default async function Page(props: DefaultPageProps) {
	return <Typography size="3xl">Coming soon</Typography>;
}
